FROM node:20-slim

RUN npm install -g supergateway oura-ring-mcp

EXPOSE 8000
CMD ["supergateway", "--stdio", "oura-ring-mcp", "--port", "8000", "--outputTransport", "streamableHttp", "--healthEndpoint", "/healthz"]
